const parentContainer = document.querySelector(".container");
const buttons = document.querySelectorAll(".btn");
const login = document.getElementById("login");

export { parentContainer, buttons, login };
